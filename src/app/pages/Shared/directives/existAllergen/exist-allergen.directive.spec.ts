import { ExistAllergenDirective } from './exist-allergen.directive';

describe('ExistAllergenDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistAllergenDirective();
    expect(directive).toBeTruthy();
  });
});
