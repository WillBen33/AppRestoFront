import { ExistProductDirective } from './exist-product.directive';

describe('ExistProductDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistProductDirective();
    expect(directive).toBeTruthy();
  });
});
