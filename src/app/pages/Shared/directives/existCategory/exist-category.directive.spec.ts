import { ExistCategoryDirective } from './exist-category.directive';

describe('ExistCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
