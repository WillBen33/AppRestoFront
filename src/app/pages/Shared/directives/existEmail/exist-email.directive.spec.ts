import { ExistEmailDirective } from './exist-email.directive';

describe('ExistEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistEmailDirective();
    expect(directive).toBeTruthy();
  });
});
