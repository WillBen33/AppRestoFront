import { ExistPhoneNumberDirective } from '../exist-phone-number/exist-phone-number.directive';

describe('ExistPhoneNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistPhoneNumberDirective();
    expect(directive).toBeTruthy();
  });
});
