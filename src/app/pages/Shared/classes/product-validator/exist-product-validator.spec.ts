import { ExistProductValidator } from './exist-product-validator';

describe('ExistProductValidator', () => {
  it('should create an instance', () => {
    expect(new ExistProductValidator()).toBeTruthy();
  });
});
