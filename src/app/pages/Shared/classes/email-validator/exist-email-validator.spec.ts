import { ExistEmailValidator } from './exist-email-validator';

/* describe('ExistEmailValidator', () => {
  it('should create an instance', () => {
    expect(new ExistEmailValidator()).toBeTruthy();
  });
});
 */