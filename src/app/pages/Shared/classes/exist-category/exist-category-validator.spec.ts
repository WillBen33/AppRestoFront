import { ExistCategoryValidator } from './exist-category-validator';

describe('ExistCategoryValidator', () => {
  it('should create an instance', () => {
    expect(new ExistCategoryValidator()).toBeTruthy();
  });
});
